{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build zig",
            "type": "shell",
            "command": "zig build-exe ${file}",
            "group": "build",
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": []
        }
    ]
}